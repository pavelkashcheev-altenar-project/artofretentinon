# AOR Simulation Dashboard — fixed version

This version fixes a pandas dtype issue in the Streamlit summary table.

Run locally:

```bash
pip install -r requirements.txt
streamlit run aor_retention_dashboard.py
```
